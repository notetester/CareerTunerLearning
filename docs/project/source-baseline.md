# 문서 기준선과 사실 확인 규칙

> 이 페이지는 학습 문서가 어느 소스 시점을 설명하는지 고정한다. 기능 설명과 코드가 충돌하면 이 페이지의 우선순위에 따라 다시 확인한다.

## 현재 기준선

| 항목 | 확인값 |
| --- | --- |
| 소스 브랜치 | CareerTuner `dev` |
| 검토 커밋 | `d00a57fc8d1e3499ba6c23acec498c47ac0d5d4c` |
| 검토일 | 2026-07-13 |
| 백엔드 | Spring Boot 4.1.0 · Java 21 · MyBatis · MySQL 8 |
| 웹/모바일 공통 UI | React 19.2.7 · React DOM 19.2.7 · React Router 8.2.0 |
| 프런트 빌드 | Vite 8.1.4 · TypeScript 7.0.2 · Tailwind CSS 4.3.2 |
| 모바일 | Capacitor 8.4.1 · Android 네이티브 프로젝트 |
| 데스크톱 | C++17 · Qt 6.11+ · QML · CMake |
| 정본 스키마 | `backend/src/main/resources/db/schema.sql`의 서로 다른 `CREATE TABLE` 168개 |

테이블 수는 특정 커밋의 정적 계수다. 패치가 추가되면 바뀔 수 있으므로 면접에서는 “기준 커밋에서 168개”라고 말하고 영구 불변 숫자처럼 표현하지 않는다.

### 직전 기준선 이후 표적 확인

직전 검토 SHA `2c4b11a9` 이후 두 변경을 별도로 확인했다.

- 관리자 AI 상담의 미답 사유는 FAQ 유사도가 임계값 미만인 경우와, 임계값을 넘었지만 라우터가 상담 경로를 고른 경우를 구분해 모순된 문구를 제거했다.
- 커뮤니티 글쓰기와 스크랩 상세는 flex item의 `width: 100%`와 desktop/mobile padding을 명시해 데스크톱에서도 모바일 폭처럼 수축하던 문제를 고쳤다.

두 변경은 모델·DB·권한 계약을 바꾸지 않고 해당 관리자 설명 문구와 커뮤니티 반응형 레이아웃에만 영향을 준다.

## 무엇을 사실로 채택하는가

같은 주제의 자료가 서로 다르면 다음 순서로 판단한다.

1. 실제 요청을 처리하는 컨트롤러·서비스·매퍼·프런트 라우트
2. `schema.sql`과 배포되는 증분 DB 패치
3. 빌드 설정과 패키지 잠금 파일
4. 자동 테스트와 `docs/verification/DEMO_READINESS_LEDGER.md`
5. 모듈 README와 아키텍처 문서
6. 초기 기획, 클래스 설계안, 과거 학습 문서

초기 문서는 목표 상태를 설명할 수 있다. 반대로 코드가 존재한다는 사실만으로 운영 설정·외부 자격증명·실기기 검증까지 완료됐다고 단정할 수도 없다.

## 구현 상태를 쓰는 네 가지 표현

| 표현 | 사용 조건 | 예시 |
| --- | --- | --- |
| 구현·연결됨 | 사용자 경로에서 실제 호출되고 테스트 근거가 있음 | 프로필 버전 조회, 첨삭 이력 삭제 |
| 설정 시 활성 | 코드와 배선은 있으나 런타임 설정이나 공급자가 필요함 | 자체 모델, OAuth, RAG |
| 실험 완료 | 학습·평가 산출물은 있으나 운영 기본 경로가 아님 | 일부 LoRA, C RAG 비교 |
| 근거 미확인 | 이름이나 설정은 있으나 학습·배포 provenance가 부족함 | F `careertuner-mod`의 직접 학습 주장 |

“코드가 있다”와 “운영 기본값이다”, “학습했다”와 “공개 베이스 모델을 자체 호스팅했다”를 섞지 않는 것이 핵심이다.

## 공개 문서에서 제외하는 정보

이 저장소는 포트폴리오 학습 자료이므로 다음은 옮기지 않는다.

- API 키, 토큰, 비밀번호, 인증서 원문
- 개인 이메일, 전화번호, 실사용자 식별자
- 내부 IP, 사설 호스트, 운영 서버 접속 명령
- 로컬 장비의 절대 경로와 개인 환경 파일
- 실제 OAuth·결제 공급자 콘솔의 비밀 설정값

대신 “환경변수로 주입”, “HTTPS 반환 주소 등록 필요”, “자체 호스팅 endpoint가 설정되어야 활성”처럼 구조만 설명한다.

## 문서 갱신 절차

새 PR이 기능을 바꾸면 전체 문서를 다시 훑지 않고 다음 순서로 좁힌다.

1. 기준 SHA 이후 변경 파일을 구한다.
2. 기능별 검증 원장의 매핑으로 영향 영역과 플랫폼을 선택한다.
3. 해당 페이지의 현재 상태·근거 경로·퀴즈 해설만 갱신한다.
4. `npm run docs:check:stale`로 폐기된 문구가 돌아오지 않았는지 확인한다.
5. VitePress 빌드를 실행해 내부 링크를 모두 검증한다.
6. 이 페이지의 SHA와 검토일을 새 기준으로 올린다.

## 면접에서의 짧은 답변

> “문서와 코드가 빠르게 달라지는 문제를 줄이기 위해 소스 SHA를 문서 기준선으로 고정했습니다. 구현 상태는 런타임 소스, DB 패치, 테스트 원장 순으로 확인하고, 운영 기본·설정 시 활성·실험 완료를 구분합니다. 이후에는 변경 파일에 매핑된 기능만 재검증해 같은 전 영역 검사를 반복하지 않습니다.”

## 근거 경로

- `backend/build.gradle`
- `frontend/package.json`
- `backend/src/main/resources/db/schema.sql`
- `docs/verification/DEMO_READINESS_LEDGER.md`
- `docs/verification/demo-readiness-checks.json`
- `scripts/verification/select-demo-regression-scope.mjs`
